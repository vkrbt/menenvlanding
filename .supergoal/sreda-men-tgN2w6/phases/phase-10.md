SUPERGOAL_PHASE_START
Phase: 10 of 12 — Кластер «Эмиграция»
Task: Закрыть кластер «Эмиграция» целиком: pillar плюс сателлиты, подшить существующие статьи, решить вопрос гео-страниц
Type: brownfield, seo, content
Mandatory commands: npm run build, python3 scripts/seo-check.py
Acceptance criteria: 10
Evidence required: список файлов с запросами, прогон чек-листа, neurocheck, проверка каннибализации, подшивка существующих статей, решение по гео, git log
Depends on phases: 9

## Why

Пустая ниша со стопроцентным совпадением с УТП — русскоязычность, потеря статуса, отсутствие страховок, одиночество; никто не может занять её так же органично, и это основной канал под Google, где сейчас нулевая индексация.

## Work

- Взять кластер «Эмиграция» из `seo/core.md`.
- Написать pillar-страницу кластера: целевой запрос в H1 и первом абзаце, полное раскрытие, ссылки на все сателлиты.
- Подшить существующие статьи `emigrantskaya-depressiya` и `odinochestvo-v-emigracii` как сателлиты: добавить ссылки на pillar, привести целевые запросы в соответствие с ядром.
- Написать недостающие сателлиты до полного покрытия запросов кластера.
- Использовать фактуру из `seo/02-audience-research.md` (раздел 4: данные Re:Russia, OutRush по эмигрантам 2022+) и раздел 3.6 (специфика: страховка, язык, доступ). Ссылки на первоисточники обязательны.
- Решить вопрос гео-специфики (Варшава, Берлин, Тбилиси, Минск): либо сателлиты внутри кластера, либо отдельные гео-страницы, либо письменное решение отложить — записать в `seo/decisions.md` с обоснованием.
- Проверить каннибализацию до публикации каждого материала.
- Прогнать чек-лист качества и `neurocheck.py` по каждому материалу.
- Поставить CTA по CTA-матрице.
- Обновить `seo/content-plan.md` и `seo/state.md`. Закоммитить и запушить в `main`.

## Acceptance criteria (all must pass — verify each in transcript)

- Pillar-страница существует; целевой запрос совпадает с запросом pillar из `seo/core.md` и присутствует дословно в H1 и первом абзаце — цитаты в транскрипте
- Все запросы кластера из `seo/core.md` покрыты; непокрытых ноль — подтверждено сверкой
- Существующие `emigrantskaya-depressiya` и `odinochestvo-v-emigracii` подшиты к pillar и учтены как сателлиты — подтверждено проверкой ссылок в обе стороны
- Каждый новый материал прошёл чек-лист качества без непройденных пунктов; прогон по каждому выведен в транскрипт
- Каждый новый материал набирает ≥80% в `neurocheck.py` — вывод по каждому файлу
- Pillar ссылается на все сателлиты, каждый сателлит — на pillar; проверено командой
- Каннибализация проверена до публикации по каждому материалу; вывод сверки в транскрипте
- Решение по гео-специфике записано в `seo/decisions.md` с обоснованием — либо реализовано сателлитами, либо отложено с условием возврата
- Каждый материал ссылается на первоисточники данных об эмиграции; доступность ссылок проверена HTTP-статусом — вывод в транскрипте
- `npm run build` и `python3 scripts/seo-check.py` зелёные, новые URL в `out/sitemap.xml`, материалы запушены в `main`

## Mandatory commands (run each, surface last ~10 lines + exit code)

- `npm run build`
- `python3 scripts/seo-check.py`

## Evidence required in transcript

- Список созданных файлов с целевыми запросами
- Прогон чек-листа качества по каждому материалу
- Вывод `neurocheck.py` по каждому файлу
- Вывод проверки каннибализации до публикации
- Подтверждение подшивки двух существующих статей к pillar
- Решение по гео-страницам с обоснованием
- Вывод проверки доступности ссылок на первоисточники
- `git log --oneline -1` после пуша

## Notes

Это ключевой кластер под Google. Если по итогам фазы 1 вердикт — «нетехническая причина, возраст и ссылочное», то именно здесь надо максимально использовать нативные ссылочные возможности: материалы кластера должны быть питчабельными в диаспорные медиа. Это не значит писать под медиа вместо поиска — это значит писать так, чтобы на них хотелось сослаться.

Данные об эмигрантах 2022+ — чувствительная фактура. Цифры приводить только со ссылкой на первоисточник и с датой исследования. Не экстраполировать и не обобщать сверх того, что сказано в источнике.

Гео-страницы — соблазн быстрых побед, но у них слабое содержание при пустом сайте. Если решаешь отложить — это нормально, зафиксируй письменно.
