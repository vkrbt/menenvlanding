SUPERGOAL_PHASE_START
Phase: 1 of 10 — Инвентаризация активов и запретов
Task: Собрать, чем проект может «заплатить» за внешние размещения и что ему запрещено публиковать
Type: research, analysis
Mandatory commands: none
Acceptance criteria: 6
Evidence required: таблица активов, таблица ограничений, вывод по категории 4
Depends on phases: none

## Why

Список площадок без знания активов и запретов проекта получится выдуманным: колонка «требования
к публикации» и колонка ограничений заполняются из фактуры проекта, а не из общих соображений.

## Work

- Прочитать источники фактуры: `seo/state.md`, `seo/core.md`, `seo/plan.md`, `seo/funnel.md`,
  `components/home/Pricing.tsx`, `components/home/Hosts.tsx`, `lib/site.ts`.
- Посчитать фактическое число статей в `content/blog/` и число кластеров в `seo/_core_data.py`.
- Составить таблицу активов: что у проекта есть → какую категорию площадок это открывает →
  чем подтверждается (файл или ответ владельца от 2026-07-29).
- Составить таблицу ограничений: ограничение → откуда взялось → конкретное последствие для
  таблицы площадок.
- Зафиксировать отдельно: юрлица нет, оффлайна нет, всё в Zoom, аудитория по всему миру.
  Вывести, что именно это отсекает в категории 4.
- Зафиксировать отдельно платность (€40 за 2 встречи, €108 за 6) и её эффект: часть UGC-площадок
  трактует продвижение платной услуги как рекламу.
- Решить и записать, куда ведёт внешняя ссылка по умолчанию, с обоснованием.
- Записать всё в `.supergoal/sreda-men-self-serve-u4qMks/assets.md`.

## Acceptance criteria (all must pass — verify each in transcript)

- Перечислено не менее десяти активов проекта, у каждого указано, какую категорию площадок он открывает.
- Каждый актив подтверждён ссылкой на конкретный файл репозитория или на ответ владельца; формулировок «вероятно» и «скорее всего» в колонке подтверждения нет.
- Перечислено не менее восьми ограничений, у каждого записано конкретное последствие для таблицы площадок, а не общее пожелание.
- Явно зафиксировано отсутствие юрлица и оффлайна, и названо, что именно из категории 4 это отсекает.
- Явно зафиксирована платность с суммами и описан её эффект на модерацию UGC-площадок.
- Записано, куда ведёт внешняя ссылка по умолчанию, с обоснованием, почему не на страницу цен.

## Mandatory commands (run each, surface last ~10 lines + exit code)

- none — аналитическая фаза, файлы сайта не изменяются

## Evidence required in transcript

- Таблица активов целиком
- Таблица ограничений целиком
- Явный вывод по категории 4 и по платности

## Notes

Не изобретать активы. Если чего-то нет — так и писать. Ценность фазы в честной базе,
на которую обопрутся четыре следующие.

Позиционирование берётся из памяти проекта: «сообщество / клуб», не «группа поддержки»,
не терапия. Для внешних площадок это ограничение жёстче, чем в своём блоге, потому что чужой
модератор читает формулировку буквально и без контекста сайта.
