SUPERGOAL_PHASE_START
Phase: 9 of 10 — Сборка документа и приложение по outreach
Task: Собрать seo/linkbuilding.md в порядке ТЗ и добавить приложение о потерянном из-за отказа от переписки
Type: writing, integration
Mandatory commands: npm run build, python3 scripts/seo-check.py
Acceptance criteria: 7
Evidence required: оглавление документа, счётчик строк сводной таблицы, коды выхода команд
Depends on phases: 2, 3, 4, 5, 6, 7, 8

## Why

Всё предыдущее — рабочие файлы прогона. Владельцу нужен один самодостаточный документ,
собранный в том порядке, который задан в ТЗ.

## Work

- Собрать `seo/linkbuilding.md` строго в порядке ТЗ: шапка, сводная таблица площадок по семи
  категориям, затем разделы по порядку — приоритизация, стратегия контента, правила ссылки
  и анкоров, естественный темп, что делать с nofollow, переиспользование, помесячный план
  таблицей, метрики. После них — список «Проверить вручную перед размещением».
- Сводная таблица собирается из реестров фаз 2–4 со всеми девятью колонками ТЗ плюс колонка
  ограничений. Ни одна площадка не теряется при переносе — сверить счётчиком.
- Добавить приложение об отказе от переписки: что именно теряется, и какой минимум переписки
  дал бы максимум выгоды. Не менее пяти конкретных пунктов. Приложение отделено заголовком
  и явно помечено как не входящее в основной план.
- Убрать все ссылки на файлы прогона `.supergoal/`: документ должен читаться без них.
- Добавить шапку: дата, рамка решения владельца от 2026-07-29, оговорка про изменчивость
  правил площадок и атрибутов ссылок.
- Проставить корректные ссылки на другие документы программы: `plan.md`, `measurement.md`,
  `state.md`, `decisions.md`.
- Прогнать обязательные команды: сборка и сквозной SEO-гейт — убедиться, что новый файл
  в `seo/` ничего не сломал.

## Acceptance criteria (all must pass — verify each in transcript)

- Порядок разделов совпадает с ТЗ: сводная таблица первой, затем восемь разделов по порядку, затем помесячный план таблицей, затем список ручных проверок.
- Сводная таблица содержит девять колонок ТЗ плюс колонку ограничений, и число площадок в ней совпадает с суммой по реестрам фаз 2–4; сверка показана счётчиком.
- Присутствуют все восемь обязательных разделов ТЗ, ни один не пропущен; проверено перечислением заголовков.
- Приложение об отказе от переписки содержит не менее пяти конкретных пунктов, отделено заголовком и помечено как не входящее в основной план.
- Документ самодостаточен: grep по нему не находит вхождений `.supergoal`.
- Есть шапка с датой, рамкой решения владельца и оговоркой про изменчивость правил площадок.
- Команды `npm run build` и `python3 scripts/seo-check.py` завершились с кодом 0.

## Mandatory commands (run each, surface last ~10 lines + exit code)

- npm run build
- python3 scripts/seo-check.py

## Evidence required in transcript

- Оглавление собранного документа списком заголовков
- Счётчик строк сводной таблицы и сверка с реестрами
- Коды выхода обеих обязательных команд

## Notes

Документ большой. Это нормально: он операционный, по нему работают, а не читают залпом.
Но структура обязана быть навигируемой — оглавление в начале.

`seo/linkbuilding.md` не входит в сборку сайта: он лежит в `seo/`, как `plan.md` и `funnel.md`.
Обязательные команды тут — регрессионные: убедиться, что ничего не сломано, а не проверить документ.

Приложение — это не «а вот ещё варианты». Это честный учёт того, что решение владельца
ограничить работу самообслуживанием имеет цену, и цена названа. Писать без упрёка и без
уговоров вернуться к переписке.
